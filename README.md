This is a simple C++ program where the user can choose to either encrypt or decrypt a message by entering a 4 digit key. 
It uses 2x2 Hill Cipher. 
It only works for uppercase letters and needs to be run in a compiler after copying the code.
